import React from 'react';
import { useArchive } from '../../context/ArchiveContext';
import '../../styles/archive.css';

const Header: React.FC = () => {
  // Header content moved to global HeaderBar; keep this empty to remove
  // subtitle, small search box, dates and extra filters per request.
  return null;
};

export default Header;
